<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.learningbank</groupId>
        <artifactId>learning-bank</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>concurrency</artifactId>
    <name>concurrency</name>
    <description>Java concurrency interview-prep project: thread safety, deadlock-free transfers, virtual threads, load balancing</description>

    <!-- No extra dependencies - everything this module needs is inherited. -->

    <build>
        <plugins>
            <!-- StructuredTaskScope (java.util.concurrent) is still a preview API in
                 JDK 25 (JEP 505, fifth preview): the compiler, Surefire, Failsafe and
                 spring-boot:run all need the enable-preview flag, or the classes
                 referencing it won't compile or load.

                 This lives in the module rather than the parent on purpose: preview
                 class files are pinned to the exact JDK feature release that compiled
                 them, and the kafka and postgres modules have no reason to carry that
                 constraint. Worth knowing for an interview. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>--enable-preview</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <argLine>--enable-preview</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>--enable-preview</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
